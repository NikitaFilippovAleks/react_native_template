import React, { ReactElement } from 'react';
import { Text } from 'react-native';

// Компонент DetailsScreen
const DetailsScreen = (): ReactElement => (
  <Text>DetailsScreen Component</Text>
);

export default DetailsScreen;
